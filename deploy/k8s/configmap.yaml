apiVersion: v1
kind: ConfigMap
metadata:
  name: lowcode-config
  namespace: lowcode
data:
  DB_HOST: "mysql-service"
  DB_PORT: "3306"
  DB_NAME: "lowcode"
  REDIS_HOST: "redis-service"
  REDIS_PORT: "6379"
  JWT_EXPIRES_IN: "7d"
  UPLOAD_DIR: "/data/uploads"
  NODE_ENV: "production"
