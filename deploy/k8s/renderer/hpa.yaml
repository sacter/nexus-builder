apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: lowcode-renderer-hpa
  namespace: lowcode
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: lowcode-renderer
  minReplicas: 2
  maxReplicas: 20
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
